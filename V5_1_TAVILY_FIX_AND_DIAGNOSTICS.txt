SHIRE VILLAS AI REVENUE OS V5.1

V5.1 fixes and hardens Tavily integration:
- trims Railway API keys before use
- removes the undocumented safe_search request field
- uses Tavily search_depth=basic for the first request path
- adds AI Lead Bots -> Test Tavily Connection
- diagnostic tests both /usage and a minimal /search request without exposing the key
- supports Brave Search fallback when BRAVE_SEARCH_API_KEY is configured
- preserves V5 scoring, deduplication and human verification

After deployment:
1. /health must show 5.1.0
2. Dashboard -> AI Lead Bots -> Test Tavily Connection
3. Expected: usage_test PASS/200 and search_test PASS/200
4. Only then run Buyer Hunter.
