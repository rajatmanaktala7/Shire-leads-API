SHIRE VILLAS AI REVENUE OS V4.2 - LOGIN HANG FIX

This is a COMPLETE replacement project. It preserves all V4.1 files and features.

WHAT WAS FIXED
1. Login no longer loads the full dashboard to validate TEAM_API_KEY.
2. New fast GET /auth/check endpoint validates the key without touching PostgreSQL.
3. Frontend requests time out after 12 seconds instead of hanging indefinitely.
4. Login button shows "Checking access..." and then a clear error if needed.
5. Command Center opens immediately after successful key validation.
6. Individual dashboard modules load independently, so one slow module cannot block the whole UI.
7. Health version is now 4.2.0.

DEPLOY
Replace the complete shire-villas-api folder with this package and preserve the folder structure.
Do not change TEAM_API_KEY unless you want to.

TEST
1. Open /health -> version should be 4.2.0
2. Open /app/ in Incognito
3. Enter TEAM_API_KEY
4. It should open Command Center quickly.
5. If the key is wrong, it will explicitly show "Invalid Team API Key."
6. Direct auth test (browser alone cannot send header):
   GET /auth/check requires X-Team-Key header and returns {"ok":true,...} when correct.
